{
  "name": "tekless-queue",
  "version": "1.0.0",
  "description": "A browser extension that posts your queued drafts to X on a schedule, through your own logged-in session.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "icons": "python3 tools/make_icons.py"
  }
}
